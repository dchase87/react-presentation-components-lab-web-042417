import React from 'react'

const SimplerComponent = (props) => {
  return (
    <div className="simpler-component" onClick={props.handleClick}>I am just happy.</div>
  )
}

export default SimplerComponent
